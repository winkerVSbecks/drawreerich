{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && case \"$f\" in \"$PWD\"/*) pnpm exec prettier --write --ignore-unknown \"$f\";; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
